@using SimpleSchedulerModels

@*<div class="row">
    <div class="col col-md-2 col-sm-12">
        Actions
    </div>
    <div class="col col-md-4 col-sm-12">
        @Worker.Worker.WorkerName
    </div>
    <div class="col col-md-4 col-sm-12">
        @Worker.Worker.Executable
    </div>
    <div class="col col-md-2 col-sm-12">
        todo schedule
    </div>
</div>*@
@*
/*
<table class="table table-striped">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Executable</th>
            <th>Schedules</th>
        </tr>
    </thead>
    <tbody>
        <tr *ngFor="let wd of workerDetails" [class.d-none]="wd.worker.isActive !== active || !wd.worker.workerName.toLowerCase().includes(filterWorkerName?.toLowerCase() || '')">
            <td style="width: 90px;">
                <div class="d-flex justify-content-between">
                    <a *ngIf="wd.worker.isActive" [routerLink]="['/workers/edit', wd.worker.workerID]">
                        <i class="bi bi-pencil"></i>
                    </a>
                    <a *ngIf="wd.worker.isActive" (click)="deleteWorker(wd.worker.workerID)">
                        <i class="bi bi-trash"></i>
                    </a>
                    <a *ngIf="wd.worker.isActive" (click)="runWorker(wd.worker.workerID)">
                        <i class="bi bi-play"></i>
                    </a>
                    <a *ngIf="!wd.worker.isActive" (click)="reactivateWorker(wd.worker.workerID)" class="btn btn-sm btn-outline-primary">
                        Reactivate
                    </a>
                </div>
                <div>
                    <a [routerLink]="['../jobs', wd.worker.workerID]">Jobs</a>
                </div>
            </td>
            <td>
                <div>{{ wd.worker.workerName }}</div>
                <div [class.d-none]="!wd.parentWorker" style="font-size: smaller; font-weight: bold;">Child of [{{ wd.parentWorker?.workerName }}]</div>
                <div [class.d-none]="!wd.worker.detailedDescription" style="font-size: smaller;">{{ wd.worker.detailedDescription }}</div>
                <div [class.d-none]="!wd.worker.emailOnSuccess" style="font-size: smaller; font-style: italic;">Email: {{ wd.worker.emailOnSuccess }}</div>
                <div style="font-size: smaller;">Timeout: {{ wd.worker.timeoutMinutes }}</div>
            </td>
            <td>
                <div style="font-weight: bold;">{{ wd.worker.directoryName }}/</div>
                <div>{{ wd.worker.executable }}</div>
                <div style="font-style: italic;" [class.d-none]="!wd.worker.argumentValues">{{ wd.worker.argumentValues }}</div>
            <td>
                <ul>
                    <li *ngFor="let schedule of wd.schedules">
                        {{ getFormattedSchedule(schedule) }} 
                    </li>
                </ul>
            </td>
        </tr>
    </tbody>
</table>

*/*@
@code {

    //[Parameter, EditorRequired]
    //public WorkerDetail Worker { get; set; } = default!;
}
